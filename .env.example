# ── Backend ─────────────────────────────────────────────────────
APP_ID=                   # Application ID (any string, used for JWT signing identity)
APP_SECRET=               # Application secret (used for JWT signing)

# ── Database ───────────────────────────────────────────────────
DATABASE_URL=             # MySQL connection string (mysql://user:pass@host:port/db)

# ── Firebase (Frontend — exposed to browser via Vite) ───────────
VITE_FIREBASE_API_KEY=AIzaSyBk0_2g2WaS-H9XxhraokOYzzYTVPAxjLs
VITE_FIREBASE_AUTH_DOMAIN=catur-bae.firebaseapp.com
VITE_FIREBASE_DATABASE_URL=https://catur-bae-default-rtdb.asia-southeast1.firebasedatabase.app
VITE_FIREBASE_PROJECT_ID=catur-bae
VITE_FIREBASE_STORAGE_BUCKET=catur-bae.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=236380923197
VITE_FIREBASE_APP_ID=1:236380923197:web:6f8c59991086a2ae2d9154

# ── Firebase Admin (Backend) ────────────────────────────────────
# To generate: download serviceAccountKey.json from Firebase Console →
# base64 encode it: [Convert]::ToBase64String([IO.File]::ReadAllBytes("serviceAccountKey.json"))
# Then paste the output here:
FIREBASE_ADMIN_CREDENTIALS_JSON=  # base64-encoded serviceAccountKey.json
FIREBASE_PROJECT_ID=catur-bae
FIREBASE_DATABASE_URL=https://catur-bae-default-rtdb.asia-southeast1.firebasedatabase.app

# ── Legacy Kimi OAuth (no longer required if using Firebase) ─────
# KIMI_AUTH_URL=
# KIMI_OPEN_URL=

# ── Admin Role ──────────────────────────────────────────────────
OWNER_UNION_ID=           # firebase:UID of the app creator (gets role "admin" on first login)

